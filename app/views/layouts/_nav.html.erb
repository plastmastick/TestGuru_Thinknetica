<div class="nav nav-user">
  <span class="nav-user title"><%= link_to t('.title'), root_path %></span>

  <div class="nav-user user">
    <% if user_signed_in? %>
      <%= t('.welcome') %>, <b class="nav-user user-email"><%= current_user.full_name %></b> Guru |
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    <% elsif request.path == '/gurus/login' %>
      <%= link_to t('.registration'), new_user_registration_path %>
    <% else %>
      <%= link_to t('.login'), new_user_session_path %>
    <% end %>
  </div>
</div>
